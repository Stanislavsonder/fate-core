# Changelog

## 1.0.0

Initial release. Scaffolds a mod project shaped like `packages/example-mod/`
using `@fate-core/mod-build`'s Vite preset, gated by chosen capabilities
(`sheetComponents`/`dice`/`theme`/`translations`).
