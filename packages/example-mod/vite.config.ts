import { defineModConfig } from '@fate-core/mod-build'

/**
 * Never published — this is the integration fixture proving the whole
 * Phase 2 loading pipeline end-to-end (planning/modules-2-0/phase-2-loader-storage-devmode.md,
 * Step 6). Build it, serve dist/, install-from-URL against a real running app.
 */
export default defineModConfig()
