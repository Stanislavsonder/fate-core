import { defineModConfig } from '@fate-core/mod-build'

/**
 * Never published — the worked example (and Cypress fixture source) for the
 * `dice` capability: one extra die shape (D6) plus one extra material,
 * proving three/cannon-es resolve through FateSDK.dice at runtime. See
 * src/tests/e2e/fixtures/mods/README.md for how the built output becomes a
 * committed e2e fixture.
 */
export default defineModConfig()
