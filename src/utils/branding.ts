/**
 * The "Powered by Fate" logo is © Evil Hat Productions, LLC and is used with permission.
 *
 * Evil Hat's licensing terms forbid altering the logo's shape or using only portions of it, so
 * it must always be rendered whole: use `object-contain`, never `object-cover`, and never crop
 * or letterbox it into a shape that clips the artwork.
 *
 * The bundled file is the white, dark-background variant, so it needs inverting on the light
 * theme. The required trademark notice is carried on the Legal & Attribution page (rendered
 * from ATTRIBUTION.md).
 *
 * @see https://fate-srd.com/official-licensing-fate
 */
export const POWERED_BY_FATE_LOGO = '/powered-by-fate.svg'
