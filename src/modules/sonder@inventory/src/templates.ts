import type { Item } from './types'
import type { FateTemplates } from '@/types'

const item: Item = {
	name: '',
	description: '',
	quantity: 0,
	icon: 'Box',
	iconColor: ''
}

export default {
	item
} as FateTemplates
