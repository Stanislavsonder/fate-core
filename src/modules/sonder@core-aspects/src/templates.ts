import type { FateTemplates } from '@/types'
import type { CharacterAspect } from './types'
import { CharacterAspectType } from './types'

const aspect: CharacterAspect = {
	name: '',
	description: '',
	type: CharacterAspectType.Other
}

export default { aspect } as FateTemplates
